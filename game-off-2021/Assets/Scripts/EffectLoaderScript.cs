using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EffectLoaderScript : MonoBehaviour
{
    // using Newtonsoft.Json;
    // using System.IO;
    // BattleEffect temp = JsonConvert.DeserializeObject<BattleEffect>(fileContents);
    // var filePath = Application.persistentDataPath + "/gamedata.json";
    // string fileContents = File.ReadAllText(filePath);
    // File.WriteAllText(filePath, "{\"test\": \"hi\"}");
}
